Andy Martin frames the interior around a single move: treating floors, walls and ceilings as the most important architectural elements, then dissolving the boundaries between them. Radius curves let floors join smoothly into walls, merging planes the way surfaces merge on screen — his reference is "texture mapping," where a colour or surface can be wrapped around any shape at the press of a button. 
